extends ./_base.pug

block body
  p Hi #{firstName},
  p We received a request to reset your #{appName} password.
  .token #{otp}
  p This code expires in 10 minutes.
  p If you did not request a password reset, no further action is needed.
